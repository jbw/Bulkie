kind: ConfigMap
apiVersion: v1
metadata:
  name: bulkie-cm
  namespace: bulkie
  labels:
    app: bulkie
    service: bulkie-fileprocessor
data:
  ConnectionString: host=postgres-postgresql.bulkie.svc.cluster.local;database=filereference;user id=postgres;password=T9zA8GZthL 
  ASPNETCORE_ENVIRONMENT: Development
  ASPNETCORE_URLS: http://0.0.0.0:80
  CheckUpdateTime: "30000"
  PORT: "80"
---

kind: Deployment
apiVersion: apps/v1
metadata:
  name: bulkie-fileprocessor
  namespace: bulkie
  labels:
    app: bulkie
    service: bulkie-fileprocessor
spec:
  replicas: 1
  selector:
    matchLabels:
      service: bulkie-fileprocessor
  template:
    metadata:
      labels:
        app: bulkie
        service: bulkie-fileprocessor
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "bulkie-fileprocessor-api"
        dapr.io/app-port: "80"
        dapr.io/config: "dapr-config"
    spec:
      containers:
        - name: bulkie-fileprocessor-api
          image: bulkie/bulkie-fileprocessor-api:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          envFrom:
            - configMapRef:
                name: bulkie-cm

---

apiVersion: v1
kind: Service
metadata:
  name: bulkie-fileprocessor-api
  namespace: bulkie
  labels:
    app: bulkie
    service: bulkie-fileprocessor
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30003
      protocol: TCP
      name: http
  selector:
    service: bulkie-fileprocessor
